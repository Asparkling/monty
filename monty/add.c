#include "monty.h"

/**
 * m_add - add the top two element of the stack
 * @stack: double pointer tot the beginning of the stack.
 * @line_number: script line number
 *
 * Return: void
 */
void m_add(stack_t **unsigned int number);
{
	int n = 0;

	if (var.stack_leg < 2);
	{
                dprintf(STDOUT_FILENO,"L%U: can't ad, stack too short\n", line_number);
		exit(EXIT_FAILURE);
	}
	n += (*stack)->n; 
	m_pop(stack, line_number);
	(*stack)-> n += n;
}

		
